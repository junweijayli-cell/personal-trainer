# TrainWell / 悦练 release continuation — 7 September 2026

## Workspace and preserved work

The original `../app` checkout and its unfinished edits remain intact. Active release work is in `../release-20260907`, branch `fix/trainwell-test-billing`, based on verified GitHub main. Do not reset or overwrite the original checkout. The original billing patch was integrated and reviewed here.

## Branding release — online

Commit `e5b4cbc` completed Chinese 悦练 branding, locale-specific browser titles, shared TrainWell / 悦练 metadata and installation name, and interface heading punctuation cleanup. Existing logo assets, sessions, URLs and storage keys were retained. GitHub Pages and Cloudflare deployments succeeded.

## Gmail — completed

Supabase SMTP was saved with sender and username `trainwell.win@gmail.com`, display name `TrainWell / 悦练`, `smtp.gmail.com:465`, and the existing 60-second interval. The operator entered the App Password privately. Only the two authentication templates and subjects were updated remotely; no blanket configuration push was performed.

The single authorized email acceptance run delivered a bilingual verification email to a disposable plus-address at the same Gmail inbox. The operator entered the private code on the one-use localhost page. Incorrect and reused codes were rejected, successful verification started an exact seven-day trial, and the temporary user and cascading records were deleted. No credentials or codes are stored here.

## Membership and billing release

Account now has a prominent **Membership plans / 会员订阅** link during the free trial. `?view=membership` opens the monthly/annual plans, survives refresh, and supports returning to the account and browser history. Both plan buttons use authenticated Stripe Checkout. Existing active/past-due subscribers are directed to Manage billing. Demo checkout and resulting membership are labeled in both languages.

Backend changes include database leases and recoverable checkout operations, stable Stripe idempotency keys, open-session reuse and expiry before plan changes, authoritative subscription reconciliation, exact invoice-to-subscription association, atomic event/state/audit transactions, explicit historical price recognition, finite paid-period access, and test-mode isolation. Account switching invalidates in-flight billing requests and notices.

Applied migration: `202609070001_safe_test_billing.sql`.
Deployed affected functions: `get-billing-catalog`, `create-checkout-session`, `create-customer-portal-session`, `stripe-webhook`, `delete-account`.
The server switch is **checkout_enabled=false**, mode **test**. The public catalog was verified to return disabled with no available plans. Private `billing_test_users` allows selected operator accounts to validate the actual flow before public enablement; members cannot read or edit this table.

Stripe test account: `acct_1UCbSA2NL4JCx8SY`.
Product: `prod_VD2xgehX5URHcE`.

| Plan | Current test Price | Historical test Price (preserved) |
| --- | --- | --- |
| US$10/month | `price_1UD3Bp2NL4JCx8SYaV7EHSlA` | `price_1UCcrH2NL4JCx8SYPWiRsSeY` |
| US$60/year | `price_1UD4GG2NL4JCx8SYEfAkxttM` | `price_1UCctD2NL4JCx8SYmfJjfylk` |

All four price references are configured in Supabase's scoped Edge Function settings. Historical Prices were not archived or modified.

Webhook destination: `we_1UD4Lo2NL4JCx8SY1PQRq3mU`, “TrainWell demo membership”, endpoint `https://yvcdlrnjhhafywawuknj.supabase.co/functions/v1/stripe-webhook`, test mode, snapshot payload version `2026-08-26.dahlia` (the Dashboard's available current version). The server retrieves authoritative objects with its pinned `2025-09-30.clover` client. Selected events: customer.subscription.created/updated/deleted/paused/resumed, checkout.session.completed, invoice.paid, invoice.payment_failed.

## Validation completed

- 69 unit tests pass, including actual PostgreSQL migration, entitlement, locking, deduplication and transaction tests through PGlite.
- 44 bilingual layout and UI tests pass across 360/390/430 px and desktop, including Account → plans → both mocked Stripe redirects, refresh/back, and checkout-disabled behavior.
- 8 mocked authentication/recovery tests pass; 1 intentionally skipped flag variant.
- 6 bilingual legal-dialog checks and 9 general mobile checks pass. Specialized suites are intentionally skipped in the generic mobile configuration and run separately above.
- Lint, TypeScript, all five affected Deno entrypoints and both hosting builds pass. Cloudflare static export verification passes (151 files, about 74 MB).
- SMTP acceptance checks pass as described above.

## Remaining secure account steps and rollout

The operator has been asked to enter `STRIPE_SECRET_KEY` (a matching fresh test key) and `STRIPE_WEBHOOK_SECRET` directly in the prepared Supabase Secrets form. Never request them in chat, save them in the repository, or reveal them in logs.

After credentials are saved: configure and verify the test Customer Portal; use temporary selected test accounts to validate both plans, actual signed webhook delivery, renewal success/failure, cancellation through period end, resubscription, and portal behavior. Remove only temporary test accounts. Keep public checkout disabled until these checks pass, then enable the test-mode switch and verify both public hosting targets. Live payments, merchant/banking verification, tax decisions and mainland activation remain out of scope.

References: [Stripe webhook guidance](https://docs.stripe.com/webhooks), [Customer Portal configuration](https://docs.stripe.com/customer-management/configure-portal), [Google App Password guidance](https://support.google.com/accounts/answer/185833).
